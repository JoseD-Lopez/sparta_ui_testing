Given("have a before hook in place with name details") do
  p @users
end

Then("I can validate the information in the before hook") do
end

Given("I use a tag") do

end

Then("only this test will run") do

end
